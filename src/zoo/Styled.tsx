import styled from '@emotion/styled';

export const Title = styled.h2`
  font-size: 1.2em;
  font-weight: bold;
  margin: 0;
  user-select: none;
`;

export const Image = styled.img`
  width: 100px;
  height: 100px;
  border-radius: 5px;
  margin: 10px 0;
  pointer-events: none;
  user-select: none;
`;
